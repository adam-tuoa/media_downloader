name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

jobs:
  build:
    name: build (${{ matrix.name }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            name: windows-x64
          - os: ubuntu-22.04 # older glibc = runs on more distros
            name: linux-x64
          - os: macos-15-intel # Adam's Mac is Intel
            name: macos-x64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Build the UI
        working-directory: frontend
        run: npm ci && npm run build

      - name: Install the backend and PyInstaller
        run: pip install -e "backend[build]"

      - name: Fetch yt-dlp, ffmpeg and qjs for this platform
        run: python scripts/fetch_binaries.py

      # Windows: no PyInstaller - Defender quarantines its exes. Embeddable Python + Inno Setup.
      - name: Assemble the Windows app folder
        if: runner.os == 'Windows'
        run: python scripts/build_windows.py

      - name: Build the Windows installer
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $env:APP_VERSION = "${{ github.ref_name }}".TrimStart("v")
          if (-not $env:APP_VERSION -or $env:APP_VERSION -eq "main") { $env:APP_VERSION = "0.0.0" }
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" packaging\windows.iss

      - name: Bundle with PyInstaller
        if: runner.os != 'Windows'
        run: pyinstaller --noconfirm --clean packaging/MediaDownloader.spec

      - name: Add yt-dlp, ffmpeg and qjs to the bundle (verbatim)
        if: runner.os != 'Windows'
        run: python scripts/bundle_binaries.py

      - name: Package (macOS)
        if: runner.os == 'macOS'
        run: ditto -c -k --sequesterRsrc --keepParent "dist/Media Downloader.app" MediaDownloader-macos-x64.zip

      - name: Package (Linux)
        if: runner.os == 'Linux'
        run: tar -C dist -czf MediaDownloader-linux-x64.tar.gz MediaDownloader

      - uses: actions/upload-artifact@v4
        with:
          name: MediaDownloader-${{ matrix.name }}
          path: MediaDownloader-*.*
          if-no-files-found: error

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          name: Media Downloader ${{ github.ref_name }}
          files: artifacts/MediaDownloader-*
          body_path: packaging/RELEASE_NOTES.md
          generate_release_notes: true
